@extends('layouts.admin')
@section('title', 'Detail User')
@section('page_title', 'Detail User')
@section('content')
<div class="max-w-2xl"><div class="mb-6 flex flex-col sm:flex-row sm:items-start justify-between gap-3"><div><h1 class="text-2xl font-extrabold text-[#0B1040]">{{ $user->name }}</h1><p class="text-slate-500 text-sm mt-1">Detail akun dan role pengguna.</p></div><a href="{{ route('superadmin.users.edit', $user->id) }}" class="btn-primary btn-sm">Edit User</a></div><div class="bg-white rounded-2xl border border-slate-200 p-5 sm:p-6 shadow-sm space-y-5"><div><p class="text-xs text-slate-500">Email</p><p class="font-semibold text-[#0B1040] break-words">{{ $user->email }}</p></div><div><p class="text-xs text-slate-500">Role</p><p class="font-semibold text-[#0B1040]">{{ $user->role?->display_name ?? '—' }}</p></div><div><p class="text-xs text-slate-500">Status</p><span class="badge {{ $user->status === 'active' ? 'badge-success' : ($user->status === 'suspended' ? 'badge-error' : 'badge-gray') }}">{{ ucfirst($user->status ?? 'active') }}</span></div><div><p class="text-xs text-slate-500">Login terakhir</p><p class="font-semibold text-[#0B1040]">{{ $user->last_login_at?->format('d M Y H:i') ?? 'Belum pernah login' }}</p></div><div class="border-t border-slate-100 pt-5"><form method="POST" action="{{ route('superadmin.users.status', $user->id) }}">@csrf @method('PATCH')<button type="submit" class="btn-secondary btn-sm">{{ $user->status === 'active' ? 'Nonaktifkan akun' : 'Aktifkan akun' }}</button></form></div></div></div>
@endsection
