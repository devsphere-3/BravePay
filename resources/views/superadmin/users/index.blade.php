@extends('layouts.admin')
@section('title', 'Manajemen User')
@section('page_title', 'Semua User')
@section('content')
<div class="mb-6 flex flex-col sm:flex-row sm:items-center justify-between gap-3"><div><h1 class="text-2xl font-extrabold text-[#0B1040]">Manajemen User</h1><p class="text-slate-500 text-sm mt-1">Kelola akun, role, dan status akses pengguna.</p></div><a href="{{ route('superadmin.users.create') }}" class="btn-primary btn-sm">Tambah User</a></div>
<div class="bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden"><div class="overflow-x-auto"><table class="w-full min-w-[800px] text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-6 py-3 text-left">Nama</th><th class="px-6 py-3 text-left">Email</th><th class="px-6 py-3 text-left">Role</th><th class="px-6 py-3 text-left">Status</th><th class="px-6 py-3 text-right">Aksi</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($users as $user)<tr><td class="px-6 py-4 font-semibold text-[#0B1040]"><a class="text-[#2563EB] hover:underline" href="{{ route('superadmin.users.show', $user->id) }}">{{ $user->name }}</a></td><td class="px-6 py-4 text-slate-600">{{ $user->email }}</td><td class="px-6 py-4 text-slate-600">{{ $user->role?->display_name ?? '—' }}</td><td class="px-6 py-4"><span class="badge {{ $user->status === 'active' ? 'badge-success' : ($user->status === 'suspended' ? 'badge-error' : 'badge-gray') }}">{{ ucfirst($user->status ?? 'active') }}</span></td><td class="px-6 py-4"><div class="flex items-center justify-end gap-3"><a class="text-xs font-semibold text-[#2563EB] hover:underline" href="{{ route('superadmin.users.edit', $user->id) }}">Edit</a><form method="POST" action="{{ route('superadmin.users.status', $user->id) }}">@csrf @method('PATCH')<button type="submit" class="text-xs font-semibold {{ $user->status === 'active' ? 'text-red-600' : 'text-emerald-600' }}">{{ $user->status === 'active' ? 'Nonaktifkan' : 'Aktifkan' }}</button></form><form method="POST" action="{{ route('superadmin.users.destroy', $user->id) }}" onsubmit="return confirm('Hapus user ini?')">@csrf @method('DELETE')<button type="submit" class="text-xs font-semibold text-red-600">Hapus</button></form></div></td></tr>@empty<tr><td colspan="5" class="px-6 py-12 text-center text-slate-400">Belum ada user.</td></tr>@endforelse</tbody></table></div>@if($users->hasPages())<div class="px-6 py-4 border-t border-slate-100">{{ $users->links() }}</div>@endif</div>
@endsection
